# 项目结构说明文档

本文档详细说明 Art Design Pro 项目中每个目录和文件的功能。

## 📁 项目根目录

### 配置文件

| 文件                    | 功能说明                                            |
| ----------------------- | --------------------------------------------------- |
| `package.json`          | 项目依赖配置、脚本命令、项目元信息                  |
| `pnpm-lock.yaml`        | pnpm 依赖锁定文件，确保依赖版本一致性               |
| `vite.config.ts`        | Vite 构建工具配置，包含路径别名、构建选项、插件配置 |
| `tsconfig.json`         | TypeScript 编译配置，包含路径映射、编译选项         |
| `eslint.config.mjs`     | ESLint 代码检查配置                                 |
| `commitlint.config.cjs` | Git 提交信息规范配置                                |
| `index.html`            | 项目入口 HTML 文件                                  |

### 其他文件

| 文件/目录       | 功能说明                            |
| --------------- | ----------------------------------- |
| `LICENSE`       | 项目许可证文件                      |
| `public/`       | 静态资源目录，构建时直接复制到 dist |
| `dist/`         | 构建输出目录，包含打包后的生产文件  |
| `node_modules/` | 项目依赖包目录                      |

---

## 📁 src/ 源码目录

### 核心入口文件

| 文件       | 功能说明                                                     |
| ---------- | ------------------------------------------------------------ |
| `main.ts`  | 应用入口文件，初始化 Vue 应用、Store、Router、国际化、指令等 |
| `App.vue`  | 根组件，配置 Element Plus 国际化、主题初始化                 |
| `env.d.ts` | TypeScript 环境类型声明文件                                  |

---

## 📁 src/api/ API 接口目录

| 文件               | 功能说明                                   |
| ------------------ | ------------------------------------------ |
| `auth.ts`          | 认证相关 API（登录、注册、登出等）         |
| `system-manage.ts` | 系统管理相关 API（用户、角色、菜单管理等） |

**功能**：统一管理所有后端 API 接口请求

---

## 📁 src/assets/ 静态资源目录

### src/assets/images/ 图片资源

| 目录/文件          | 功能说明                                               |
| ------------------ | ------------------------------------------------------ |
| `common/logo.webp` | 系统 Logo                                              |
| `favicon.ico`      | 网站图标                                               |
| `settings/`        | 设置页面相关图片（菜单布局、菜单样式、主题样式预览图） |
| `svg/`             | SVG 图标（403、404、500 错误页图标、登录图标）         |
| `user/`            | 用户相关图片（头像、背景图）                           |

### src/assets/styles/ 样式文件

#### src/assets/styles/core/ 核心样式

| 文件                     | 功能说明                  |
| ------------------------ | ------------------------- |
| `app.scss`               | 应用主样式文件            |
| `reset.scss`             | CSS 重置样式              |
| `tailwind.css`           | Tailwind CSS 样式         |
| `dark.scss`              | 暗色主题样式              |
| `el-light.scss`          | Element Plus 亮色主题样式 |
| `el-dark.scss`           | Element Plus 暗色主题样式 |
| `el-ui.scss`             | Element Plus UI 组件样式  |
| `mixin.scss`             | SCSS 混入函数             |
| `theme-animation.scss`   | 主题切换动画              |
| `theme-change.scss`      | 主题变化样式              |
| `router-transition.scss` | 路由过渡动画              |

#### src/assets/styles/custom/ 自定义样式

| 文件                | 功能说明              |
| ------------------- | --------------------- |
| `one-dark-pro.scss` | One Dark Pro 主题样式 |

#### src/assets/styles/index.scss

样式入口文件，统一导入所有样式

### src/assets/svg/ SVG 资源

| 文件         | 功能说明          |
| ------------ | ----------------- |
| `loading.ts` | 加载动画 SVG 配置 |

---

## 📁 src/components/ 组件目录

### src/components/core/ 核心组件

#### src/components/core/base/ 基础组件

| 组件            | 功能说明     |
| --------------- | ------------ |
| `art-logo/`     | Logo 组件    |
| `art-svg-icon/` | SVG 图标组件 |

#### src/components/core/charts/ 图表组件

| 组件              | 功能说明   |
| ----------------- | ---------- |
| `art-bar-chart/`  | 柱状图组件 |
| `art-line-chart/` | 折线图组件 |

#### src/components/core/forms/ 表单组件

| 组件                | 功能说明     |
| ------------------- | ------------ |
| `art-button-more/`  | 更多按钮组件 |
| `art-button-table/` | 表格按钮组件 |
| `art-drag-verify/`  | 拖拽验证组件 |
| `art-form/`         | 表单组件     |
| `art-search-bar/`   | 搜索栏组件   |

#### src/components/core/layouts/ 布局组件

| 组件                    | 功能说明                           |
| ----------------------- | ---------------------------------- |
| `art-breadcrumb/`       | 面包屑导航组件                     |
| `art-global-component/` | 全局组件容器                       |
| `art-header-bar/`       | 顶部栏组件（包含用户菜单、通知等） |
| `art-menus/`            | 菜单组件（支持多种布局和样式）     |
| `art-notification/`     | 通知面板组件                       |
| `art-page-content/`     | 页面内容容器组件                   |
| `art-settings-panel/`   | 设置面板组件（主题、布局等配置）   |
| `art-work-tab/`         | 工作标签页组件                     |

#### src/components/core/others/ 其他组件

| 组件              | 功能说明         |
| ----------------- | ---------------- |
| `art-menu-right/` | 菜单右键菜单组件 |

#### src/components/core/tables/ 表格组件

| 组件                | 功能说明                            |
| ------------------- | ----------------------------------- |
| `art-table/`        | 表格组件（封装 Element Plus Table） |
| `art-table-header/` | 表格头部组件                        |

#### src/components/core/text-effect/ 文字效果组件

| 组件            | 功能说明         |
| --------------- | ---------------- |
| `art-count-to/` | 数字滚动动画组件 |

#### src/components/core/theme/ 主题组件

| 组件         | 功能说明      |
| ------------ | ------------- |
| `theme-svg/` | 主题 SVG 组件 |

#### src/components/core/views/ 视图组件

| 组件         | 功能说明                      |
| ------------ | ----------------------------- |
| `exception/` | 异常页面组件（403、404、500） |
| `login/`     | 登录页面组件                  |
| `result/`    | 结果页面组件（成功/失败）     |

#### src/components/core/widget/ 小部件组件

| 组件               | 功能说明     |
| ------------------ | ------------ |
| `art-icon-button/` | 图标按钮组件 |

---

## 📁 src/config/ 配置目录

### src/config/modules/ 配置模块

| 文件           | 功能说明       |
| -------------- | -------------- |
| `component.ts` | 全局组件配置   |
| `headerBar.ts` | 顶部栏功能配置 |

### src/config/assets/ 资源配置

| 文件        | 功能说明         |
| ----------- | ---------------- |
| `images.ts` | 图片资源路径配置 |

### 配置文件

| 文件         | 功能说明                       |
| ------------ | ------------------------------ |
| `index.ts`   | 配置入口文件，导出系统全局配置 |
| `setting.ts` | 系统设置默认配置               |

**功能**：集中管理系统的所有配置项，包括主题、菜单、颜色等

---

## 📁 src/directives/ 指令目录

### src/directives/core/ 核心指令

| 文件       | 功能说明                              |
| ---------- | ------------------------------------- |
| `auth.ts`  | 权限指令（v-auth），控制元素显示/隐藏 |
| `roles.ts` | 角色指令（v-roles），基于角色控制访问 |

### src/directives/index.ts

指令入口文件，统一注册所有指令

**功能**：提供 Vue 自定义指令，用于权限控制、角色验证等

---

## 📁 src/enums/ 枚举目录

| 文件          | 功能说明                           |
| ------------- | ---------------------------------- |
| `appEnum.ts`  | 应用枚举（主题、菜单类型、布局等） |
| `formEnum.ts` | 表单枚举（表单类型、验证规则等）   |

**功能**：定义项目中使用的常量枚举值

---

## 📁 src/hooks/ 组合式函数目录

### src/hooks/core/ 核心 Hooks

| 文件                 | 功能说明                                     |
| -------------------- | -------------------------------------------- |
| `useAppMode.ts`      | 应用模式管理（开发/生产）                    |
| `useAuth.ts`         | 认证相关逻辑（登录、登出、权限检查）         |
| `useChart.ts`        | 图表管理（ECharts 封装，生命周期、主题适配） |
| `useCommon.ts`       | 通用工具函数                                 |
| `useHeaderBar.ts`    | 顶部栏功能逻辑                               |
| `useLayoutHeight.ts` | 布局高度计算                                 |
| `useTable.ts`        | 表格管理（数据、分页、排序等）               |
| `useTableColumns.ts` | 表格列管理（动态列、列配置）                 |
| `useTableHeight.ts`  | 表格高度计算                                 |
| `useTheme.ts`        | 主题管理（亮色/暗色切换）                    |

### src/hooks/index.ts

Hooks 入口文件，统一导出所有 Hooks

**功能**：提供可复用的组合式函数，封装业务逻辑

---

## 📁 src/locales/ 国际化目录

| 文件            | 功能说明                        |
| --------------- | ------------------------------- |
| `index.ts`      | 国际化配置入口，初始化 vue-i18n |
| `langs/zh.json` | 中文语言包                      |
| `langs/en.json` | 英文语言包                      |

**功能**：多语言支持，管理所有文本翻译

---

## 📁 src/mock/ Mock 数据目录

| 文件               | 功能说明      |
| ------------------ | ------------- |
| `temp/formData.ts` | 表单数据 Mock |

**功能**：提供 Mock 数据，用于开发和测试

---

## 📁 src/plugins/ 插件目录

| 文件         | 功能说明                               |
| ------------ | -------------------------------------- |
| `index.ts`   | 插件入口文件                           |
| `echarts.ts` | ECharts 插件配置（按需导入图表和组件） |

**功能**：配置和初始化第三方插件

---

## 📁 src/router/ 路由目录

### src/router/core/ 路由核心模块

| 文件                          | 功能说明                       |
| ----------------------------- | ------------------------------ |
| `index.ts`                    | 路由核心模块入口               |
| `ComponentLoader.ts`          | 组件加载器（动态加载路由组件） |
| `IframeRouteManager.ts`       | Iframe 路由管理器              |
| `MenuProcessor.ts`            | 菜单处理器（处理菜单数据）     |
| `RoutePermissionValidator.ts` | 路由权限验证器                 |
| `RouteRegistry.ts`            | 路由注册器                     |
| `RouteTransformer.ts`         | 路由转换器（转换路由配置）     |
| `RouteValidator.ts`           | 路由验证器                     |

### src/router/guards/ 路由守卫

| 文件            | 功能说明                                       |
| --------------- | ---------------------------------------------- |
| `beforeEach.ts` | 路由前置守卫（权限验证、动态路由加载）         |
| `afterEach.ts`  | 路由后置守卫（页面标题设置、工作标签页管理等） |

### src/router/modules/ 路由模块

| 文件           | 功能说明                         |
| -------------- | -------------------------------- |
| `index.ts`     | 路由模块入口                     |
| `dashboard.ts` | 仪表盘路由                       |
| `exception.ts` | 异常页面路由（403、404、500）    |
| `result.ts`    | 结果页面路由                     |
| `system.ts`    | 系统管理路由（用户、角色、菜单） |

### src/router/routes/ 路由配置

| 文件              | 功能说明                     |
| ----------------- | ---------------------------- |
| `staticRoutes.ts` | 静态路由配置（登录、404 等） |
| `asyncRoutes.ts`  | 异步路由配置（动态路由）     |

### 其他文件

| 文件             | 功能说明                   |
| ---------------- | -------------------------- |
| `index.ts`       | 路由入口文件，创建路由实例 |
| `routesAlias.ts` | 路由别名配置               |

**功能**：管理应用的所有路由配置、权限控制、动态加载等

---

## 📁 src/store/ 状态管理目录

### src/store/modules/ Store 模块

| 文件         | 功能说明                                   |
| ------------ | ------------------------------------------ |
| `menu.ts`    | 菜单状态管理（菜单数据、当前菜单等）       |
| `setting.ts` | 系统设置状态管理（主题、布局、菜单样式等） |
| `table.ts`   | 表格状态管理（表格配置缓存）               |
| `user.ts`    | 用户状态管理（用户信息、token、权限等）    |
| `worktab.ts` | 工作标签页状态管理（打开的标签页列表）     |

### src/store/index.ts

Store 入口文件，创建 Pinia 实例，配置持久化插件

**功能**：全局状态管理，使用 Pinia，支持数据持久化

---

## 📁 src/types/ 类型定义目录

### src/types/api/ API 类型

| 文件       | 功能说明                          |
| ---------- | --------------------------------- |
| `api.d.ts` | API 接口类型定义（请求/响应类型） |

### src/types/common/ 通用类型

| 文件          | 功能说明         |
| ------------- | ---------------- |
| `index.ts`    | 通用类型定义     |
| `response.ts` | 响应数据类型定义 |

### src/types/component/ 组件类型

| 文件       | 功能说明         |
| ---------- | ---------------- |
| `chart.ts` | 图表组件类型定义 |
| `index.ts` | 组件类型定义     |

### src/types/config/ 配置类型

| 文件       | 功能说明     |
| ---------- | ------------ |
| `index.ts` | 配置类型定义 |

### src/types/import/ 自动导入类型

| 文件                | 功能说明                                              |
| ------------------- | ----------------------------------------------------- |
| `auto-imports.d.ts` | 自动导入的 API 类型（由 unplugin-auto-import 生成）   |
| `components.d.ts`   | 自动导入的组件类型（由 unplugin-vue-components 生成） |

### src/types/router/ 路由类型

| 文件       | 功能说明     |
| ---------- | ------------ |
| `index.ts` | 路由类型定义 |

### src/types/store/ Store 类型

| 文件       | 功能说明       |
| ---------- | -------------- |
| `index.ts` | Store 类型定义 |

### src/types/index.ts

类型定义入口文件

**功能**：TypeScript 类型定义，提供类型安全和代码提示

---

## 📁 src/utils/ 工具函数目录

### src/utils/form/ 表单工具

| 文件            | 功能说明       |
| --------------- | -------------- |
| `index.ts`      | 表单工具函数   |
| `responsive.ts` | 响应式表单工具 |

### src/utils/http/ HTTP 工具

| 文件        | 功能说明                    |
| ----------- | --------------------------- |
| `index.ts`  | HTTP 请求封装（基于 Axios） |
| `error.ts`  | HTTP 错误处理               |
| `status.ts` | HTTP 状态码处理             |

### src/utils/navigation/ 导航工具

| 文件         | 功能说明       |
| ------------ | -------------- |
| `index.ts`   | 导航工具入口   |
| `jump.ts`    | 页面跳转工具   |
| `route.ts`   | 路由工具       |
| `worktab.ts` | 工作标签页工具 |

### src/utils/router.ts

路由工具函数

### src/utils/storage/ 存储工具

| 文件                     | 功能说明                                    |
| ------------------------ | ------------------------------------------- |
| `index.ts`               | 存储工具入口                                |
| `storage.ts`             | 存储操作封装（localStorage/sessionStorage） |
| `storage-config.ts`      | 存储配置                                    |
| `storage-key-manager.ts` | 存储键管理器（版本化管理）                  |

### src/utils/sys/ 系统工具

| 文件              | 功能说明              |
| ----------------- | --------------------- |
| `index.ts`        | 系统工具入口          |
| `error-handle.ts` | 全局错误处理          |
| `mittBus.ts`      | 事件总线（基于 mitt） |

### src/utils/table/ 表格工具

| 文件             | 功能说明     |
| ---------------- | ------------ |
| `tableCache.ts`  | 表格缓存管理 |
| `tableConfig.ts` | 表格配置管理 |
| `tableUtils.ts`  | 表格工具函数 |

### src/utils/ui/ UI 工具

| 文件           | 功能说明     |
| -------------- | ------------ |
| `index.ts`     | UI 工具入口  |
| `animation.ts` | 主题动画工具 |
| `colors.ts`    | 颜色工具     |
| `loading.ts`   | 加载状态管理 |

### src/utils/index.ts

工具函数入口文件

**功能**：提供各种工具函数，包括 HTTP 请求、存储、路由、表单验证等

---

## 📁 src/views/ 视图页面目录

### src/views/auth/ 认证页面

| 页面               | 功能说明     |
| ------------------ | ------------ |
| `login/`           | 登录页面     |
| `register/`        | 注册页面     |
| `forget-password/` | 忘记密码页面 |

### src/views/dashboard/ 仪表盘页面

| 页面       | 功能说明                                             |
| ---------- | ---------------------------------------------------- |
| `console/` | 控制台页面（包含卡片列表、活跃用户、销售概览等模块） |

### src/views/exception/ 异常页面

| 页面   | 功能说明           |
| ------ | ------------------ |
| `403/` | 403 无权限页面     |
| `404/` | 404 未找到页面     |
| `500/` | 500 服务器错误页面 |

### src/views/index/ 首页

| 页面        | 功能说明                               |
| ----------- | -------------------------------------- |
| `index.vue` | 应用主页面（包含布局、菜单、内容区等） |

### src/views/result/ 结果页面

| 页面       | 功能说明     |
| ---------- | ------------ |
| `success/` | 成功结果页面 |
| `fail/`    | 失败结果页面 |

### src/views/system/ 系统管理页面

| 页面           | 功能说明     |
| -------------- | ------------ |
| `menu/`        | 菜单管理页面 |
| `role/`        | 角色管理页面 |
| `user/`        | 用户管理页面 |
| `user-center/` | 用户中心页面 |

**功能**：应用的所有页面视图，对应路由配置

---

## 📋 目录结构总结

```
art-design-pro/
├── src/                    # 源码目录
│   ├── api/                # API 接口
│   ├── assets/             # 静态资源（图片、样式、SVG）
│   ├── components/         # Vue 组件
│   ├── config/             # 配置文件
│   ├── directives/         # Vue 指令
│   ├── enums/              # 枚举定义
│   ├── hooks/              # 组合式函数
│   ├── locales/            # 国际化
│   ├── mock/               # Mock 数据
│   ├── plugins/            # 插件配置
│   ├── router/             # 路由配置
│   ├── store/              # 状态管理
│   ├── types/              # 类型定义
│   ├── utils/              # 工具函数
│   ├── views/              # 页面视图
│   ├── App.vue             # 根组件
│   └── main.ts             # 入口文件
├── public/                 # 静态资源（构建时直接复制）
├── dist/                   # 构建输出目录
├── node_modules/           # 依赖包
├── vite.config.ts          # Vite 配置
├── tsconfig.json           # TypeScript 配置
├── package.json            # 项目配置
└── index.html              # HTML 入口
```

---

## 🔑 关键文件说明

### 入口文件

- `src/main.ts` - 应用入口，初始化所有模块
- `index.html` - HTML 入口文件

### 配置文件

- `vite.config.ts` - 构建配置
- `tsconfig.json` - TypeScript 配置
- `src/config/index.ts` - 系统全局配置

### 核心模块

- `src/router/index.ts` - 路由初始化
- `src/store/index.ts` - 状态管理初始化
- `src/locales/index.ts` - 国际化初始化

---

## 📝 使用说明

1. **开发环境**：运行 `npm run dev` 启动开发服务器
2. **构建生产**：运行 `npm run build` 构建生产版本
3. **代码检查**：运行 `npm run lint` 检查代码规范
4. **代码格式化**：运行 `npm run fix` 自动修复代码格式

---

## 🔗 相关文档

- [Vite 官方文档](https://vitejs.dev/)
- [Vue 3 官方文档](https://cn.vuejs.org/)
- [Element Plus 文档](https://element-plus.org/)
- [Pinia 文档](https://pinia.vuejs.org/)
- [Vue Router 文档](https://router.vuejs.org/)
- [项目文档](https://www.artd.pro/docs/zh/guide/introduce.html)

---

**最后更新**：2024-12-03
