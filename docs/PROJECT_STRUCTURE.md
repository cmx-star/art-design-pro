# 项目结构说明文档

本文档详细说明 Art Design Pro 项目中每个目录和文件的功能。

## 📁 项目目录结构

```
art-design-pro/
├── src/                      # 源码目录
│   ├── api/                  # API 接口相关代码
│   │   ├── auth.ts           # 认证相关的 API 接口定义（如登录、注册、用户信息）
│   │   ├── device.ts         # 设备管理相关的 API 接口定义（如网口管理）
│   │   └── system-manage.ts   # 系统管理相关的 API 接口定义（如菜单、用户、角色管理）
│   │
│   ├── App.vue               # Vue 根组件，定义应用的全局结构和入口
│   │
│   ├── assets/               # 静态资源目录
│   │   ├── images/           # 图片资源目录
│   │   │   ├── common/       # 通用图片资源（Logo 等）
│   │   │   ├── settings/     # 设置页面相关图片（菜单布局、菜单样式、主题样式预览图）
│   │   │   ├── svg/          # SVG 图标（403、404、500 错误页图标、登录图标）
│   │   │   └── user/         # 用户相关图片（头像、背景图）
│   │   ├── styles/           # 全局样式文件
│   │   │   ├── core/         # 核心样式（系统级样式）
│   │   │   │   ├── app.scss              # 应用主样式文件
│   │   │   │   ├── reset.scss            # CSS 重置样式
│   │   │   │   ├── tailwind.css          # Tailwind CSS 样式
│   │   │   │   ├── dark.scss             # 暗色主题样式
│   │   │   │   ├── el-light.scss         # Element Plus 亮色主题样式
│   │   │   │   ├── el-dark.scss          # Element Plus 暗色主题样式
│   │   │   │   ├── el-ui.scss            # Element Plus UI 组件样式
│   │   │   │   ├── mixin.scss            # SCSS 混入函数
│   │   │   │   ├── theme-animation.scss  # 主题切换动画
│   │   │   │   ├── theme-change.scss     # 主题变化样式
│   │   │   │   └── router-transition.scss # 路由过渡动画
│   │   │   ├── custom/        # 自定义样式（业务级样式）
│   │   │   │   └── one-dark-pro.scss      # One Dark Pro 主题样式
│   │   │   └── index.scss     # 样式入口文件
│   │   └── svg/               # SVG 相关资源
│   │       └── loading.ts     # 加载动画 SVG 定义
│   │
│   ├── components/           # 组件目录
│   │   └── core/             # 核心组件（系统级通用组件库）
│   │       ├── base/         # 基础组件
│   │       │   ├── art-logo/        # Logo 组件
│   │       │   └── art-svg-icon/    # SVG 图标组件
│   │       ├── charts/       # 图表组件
│   │       │   ├── art-bar-chart/   # 柱状图组件
│   │       │   └── art-line-chart/  # 折线图组件
│   │       ├── forms/        # 表单组件
│   │       │   ├── art-button-more/  # 更多按钮组件
│   │       │   ├── art-button-table/ # 表格按钮组件
│   │       │   ├── art-form/         # 表单组件
│   │       │   └── art-search-bar/   # 搜索栏组件
│   │       ├── layouts/      # 布局组件
│   │       │   ├── art-breadcrumb/      # 面包屑导航组件
│   │       │   ├── art-global-component/ # 全局组件容器
│   │       │   ├── art-header-bar/       # 顶部栏组件（包含用户菜单、通知等）
│   │       │   ├── art-menus/            # 菜单组件（支持多种布局和样式）
│   │       │   │   ├── art-horizontal-menu/ # 水平菜单
│   │       │   │   ├── art-mixed-menu/      # 混合菜单
│   │       │   │   └── art-sidebar-menu/    # 侧边栏菜单
│   │       │   ├── art-notification/     # 通知面板组件
│   │       │   ├── art-page-content/     # 页面内容容器组件
│   │       │   ├── art-settings-panel/    # 设置面板组件（主题、布局等配置）
│   │       │   └── art-work-tab/          # 工作标签页组件
│   │       ├── others/       # 其他组件
│   │       │   └── art-menu-right/        # 菜单右键菜单组件
│   │       ├── tables/       # 表格组件
│   │       │   ├── art-table/        # 表格组件（封装 Element Plus Table）
│   │       │   └── art-table-header/  # 表格头部组件
│   │       ├── text-effect/  # 文本特效组件
│   │       │   └── art-count-to/      # 数字滚动动画组件
│   │       ├── theme/        # 主题相关组件
│   │       │   └── theme-svg/         # 主题 SVG 组件
│   │       ├── views/        # 视图组件
│   │       │   ├── exception/        # 异常页面组件（403、404、500）
│   │       │   ├── login/             # 登录页面组件
│   │       │   └── result/            # 结果页面组件（成功/失败）
│   │       └── widget/       # 小部件组件
│   │           └── art-icon-button/   # 图标按钮组件
│   │
│   ├── config/                # 项目配置目录
│   │   ├── assets/           # 静态资源配置
│   │   │   └── images.ts     # 图片资源路径配置
│   │   ├── modules/          # 模块化配置
│   │   │   ├── component.ts  # 组件配置
│   │   │   └── headerBar.ts  # 顶部栏配置
│   │   ├── index.ts          # 配置入口文件
│   │   └── setting.ts        # 系统设置配置
│   │
│   ├── directives/           # Vue 自定义指令
│   │   ├── core/             # 核心指令
│   │   │   ├── auth.ts       # 认证指令（v-auth，控制元素显示/隐藏）
│   │   │   └── roles.ts      # 角色权限指令（v-roles，基于角色控制访问）
│   │   └── index.ts          # 指令入口文件
│   │
│   ├── enums/                # 枚举定义
│   │   ├── appEnum.ts        # 应用级枚举（如主题类型、语言类型、菜单类型、布局等）
│   │   └── formEnum.ts       # 表单相关枚举（如表单类型、验证规则等）
│   │
│   ├── env.d.ts              # TypeScript 环境声明文件
│   │
│   ├── hooks/                # Vue 3 Composable 函数（可复用逻辑）
│   │   ├── core/             # 核心 Hooks
│   │   │   ├── useAppMode.ts      # 应用模式相关逻辑（开发/生产）
│   │   │   ├── useAuth.ts         # 认证相关逻辑（登录、登出、权限检查）
│   │   │   ├── useChart.ts        # 图表相关逻辑（ECharts 封装，生命周期、主题适配）
│   │   │   ├── useCommon.ts       # 通用逻辑
│   │   │   ├── useHeaderBar.ts    # 顶部栏逻辑
│   │   │   ├── useLayoutHeight.ts # 布局高度计算逻辑
│   │   │   ├── useTable.ts        # 表格逻辑（数据、分页、排序等）
│   │   │   ├── useTableColumns.ts # 表格列配置逻辑（动态列、列配置）
│   │   │   ├── useTableHeight.ts  # 表格高度计算逻辑
│   │   │   └── useTheme.ts        # 主题切换逻辑（亮色/暗色切换）
│   │   └── index.ts          # Hooks 入口文件
│   │
│   ├── locales/              # 国际化（i18n）资源
│   │   ├── index.ts          # 国际化入口文件
│   │   └── langs/            # 多语言文件
│   │       ├── en.json       # 英文语言包
│   │       └── zh.json       # 中文语言包
│   │
│   ├── main.ts               # 项目主入口文件
│   │
│   ├── mock/                 # Mock 数据目录
│   │   ├── device/           # 设备管理 Mock 数据
│   │   │   └── port.ts       # 网口管理 Mock 数据
│   │   ├── temp/             # 临时 Mock 数据
│   │   │   └── formData.ts   # 表单数据
│   │   └── upgrade/           # 更新日志数据（预留目录）
│   │
│   ├── plugins/              # 插件配置
│   │   ├── echarts.ts        # ECharts 图表库配置（按需导入图表和组件）
│   │   └── index.ts          # 插件入口文件
│   │
│   ├── router/               # Vue Router 路由相关代码
│   │   ├── core/             # 路由核心功能
│   │   │   ├── ComponentLoader.ts          # 组件加载器（动态加载路由组件）
│   │   │   ├── IframeRouteManager.ts       # Iframe 路由管理器
│   │   │   ├── MenuProcessor.ts            # 菜单处理器（处理菜单数据）
│   │   │   ├── RoutePermissionValidator.ts # 路由权限验证器
│   │   │   ├── RouteRegistry.ts            # 路由注册器
│   │   │   ├── RouteTransformer.ts         # 路由转换器（转换路由配置）
│   │   │   ├── RouteValidator.ts           # 路由验证器
│   │   │   └── index.ts                    # 核心功能入口
│   │   ├── guards/           # 路由守卫
│   │   │   ├── afterEach.ts  # 全局后置守卫（页面标题设置、工作标签页管理等）
│   │   │   └── beforeEach.ts # 全局前置守卫（权限验证、动态路由加载）
│   │   ├── modules/          # 路由模块定义
│   │   │   ├── dashboard.ts  # 仪表盘路由
│   │   │   ├── device.ts    # 设备管理路由
│   │   │   ├── exception.ts # 异常页面路由（403、404、500）
│   │   │   ├── index.ts     # 路由模块入口
│   │   │   ├── result.ts    # 结果页面路由
│   │   │   └── system.ts    # 系统管理路由（用户、角色、菜单）
│   │   ├── routes/          # 路由配置
│   │   │   ├── asyncRoutes.ts  # 异步路由（动态路由）
│   │   │   └── staticRoutes.ts # 静态路由（固定路由）
│   │   ├── index.ts          # 路由主入口
│   │   └── routesAlias.ts    # 路由别名定义
│   │
│   ├── store/                # Pinia 状态管理
│   │   ├── modules/          # 状态管理模块
│   │   │   ├── menu.ts       # 菜单状态管理（菜单数据、当前菜单等）
│   │   │   ├── setting.ts    # 设置状态管理（主题、布局、菜单样式等）
│   │   │   ├── table.ts      # 表格状态管理（表格配置缓存）
│   │   │   ├── user.ts       # 用户状态管理（用户信息、token、权限等）
│   │   │   └── worktab.ts    # 工作标签页状态管理（打开的标签页列表）
│   │   └── index.ts          # Pinia 入口文件
│   │
│   ├── types/                # TypeScript 类型定义
│   │   ├── api/              # API 相关类型
│   │   │   └── api.d.ts      # API 接口类型定义（请求/响应类型）
│   │   ├── common/           # 通用类型定义
│   │   │   ├── index.ts      # 通用类型入口
│   │   │   └── response.ts   # 响应类型定义
│   │   ├── component/        # 组件相关类型
│   │   │   ├── chart.ts      # 图表组件类型
│   │   │   └── index.ts      # 组件类型入口
│   │   ├── config/           # 配置相关类型
│   │   │   └── index.ts      # 配置类型定义
│   │   ├── import/           # 自动导入类型声明
│   │   │   ├── auto-imports.d.ts # 自动导入的函数类型（由 unplugin-auto-import 生成）
│   │   │   └── components.d.ts   # 自动导入的组件类型（由 unplugin-vue-components 生成）
│   │   ├── router/           # 路由相关类型
│   │   │   └── index.ts      # 路由类型定义
│   │   ├── store/            # 状态管理相关类型
│   │   │   └── index.ts      # Store 类型定义
│   │   └── index.ts          # 类型定义总入口
│   │
│   ├── utils/                # 工具函数目录
│   │   ├── form/             # 表单相关工具
│   │   │   ├── index.ts      # 表单工具入口
│   │   │   └── responsive.ts # 响应式表单工具
│   │   ├── http/             # HTTP 请求工具
│   │   │   ├── error.ts      # 错误处理
│   │   │   ├── index.ts      # HTTP 工具入口（基于 Axios）
│   │   │   └── status.ts     # 状态码处理
│   │   ├── navigation/      # 导航相关工具
│   │   │   ├── index.ts      # 导航工具入口
│   │   │   ├── jump.ts       # 页面跳转工具
│   │   │   ├── route.ts      # 路由工具
│   │   │   └── worktab.ts    # 工作标签页工具
│   │   ├── storage/          # 存储相关工具
│   │   │   ├── index.ts              # 存储工具入口
│   │   │   ├── storage-config.ts     # 存储配置
│   │   │   ├── storage-key-manager.ts # 存储键管理器（版本化管理）
│   │   │   └── storage.ts            # 存储工具实现（localStorage/sessionStorage）
│   │   ├── sys/              # 系统相关工具
│   │   │   ├── error-handle.ts # 全局错误处理
│   │   │   ├── index.ts        # 系统工具入口
│   │   │   └── mittBus.ts       # 事件总线（基于 mitt）
│   │   ├── table/            # 表格相关工具
│   │   │   ├── tableCache.ts  # 表格缓存管理
│   │   │   ├── tableConfig.ts # 表格配置管理
│   │   │   └── tableUtils.ts  # 表格工具函数
│   │   ├── ui/               # UI 相关工具
│   │   │   ├── animation.ts   # 主题动画工具
│   │   │   ├── colors.ts      # 颜色工具
│   │   │   ├── index.ts       # UI 工具入口
│   │   │   └── loading.ts     # 加载状态管理
│   │   ├── index.ts          # 工具函数总入口
│   │   └── router.ts         # 路由工具函数
│   │
│   └── views/                # 页面组件目录
│       ├── auth/             # 认证页面
│       │   ├── login/        # 登录页面
│       │   ├── register/     # 注册页面
│       │   └── forget-password/ # 忘记密码页面
│       ├── dashboard/        # 仪表盘页面
│       │   └── console/      # 控制台页面（包含卡片列表、活跃用户、销售概览等模块）
│       ├── device/           # 设备管理页面
│       │   └── port/         # 网口管理页面
│       ├── exception/        # 异常页面
│       │   ├── 403/          # 403 无权限页面
│       │   ├── 404/          # 404 未找到页面
│       │   └── 500/          # 500 服务器错误页面
│       ├── index/            # 首页
│       │   └── index.vue     # 应用主页面（包含布局、菜单、内容区等）
│       ├── result/           # 结果页面
│       │   ├── success/      # 成功结果页面
│       │   └── fail/         # 失败结果页面
│       └── system/           # 系统管理页面
│           ├── menu/         # 菜单管理页面
│           ├── role/         # 角色管理页面
│           ├── user/         # 用户管理页面
│           └── user-center/ # 用户中心页面
│
├── public/                   # 静态资源目录（构建时直接复制到 dist）
│   └── favicon.ico           # 网站图标
│
├── docs/                     # 项目文档目录
│   ├── PROJECT_STRUCTURE.md  # 项目结构说明文档
│   └── DEV_EXAMPLE_PORT_MANAGEMENT.md # 开发示例文档（网口管理功能）
│
├── dist/                     # 构建输出目录（包含打包后的生产文件）
│
├── node_modules/             # 项目依赖包目录
│
├── tsconfig.json             # TypeScript 配置文件
├── vite.config.ts            # Vite 配置文件
├── eslint.config.mjs         # ESLint 代码检查配置
├── commitlint.config.cjs     # Git 提交信息规范配置
├── package.json              # 项目依赖配置、脚本命令、项目元信息
├── pnpm-lock.yaml            # pnpm 依赖锁定文件，确保依赖版本一致性
└── index.html                # 项目入口 HTML 文件
```

## 📋 目录功能说明

### 核心目录

#### `src/api/` - API 接口管理

统一管理所有后端 API 接口请求，包括认证、系统管理、设备管理等模块的接口定义。

#### `src/components/` - Vue 组件

- **core/** - 核心组件库，包含基础组件、表单组件、布局组件、表格组件等系统级通用组件
- 所有组件采用 `art-` 前缀命名规范

#### `src/config/` - 项目配置

集中管理系统的所有配置项，包括主题、菜单、颜色、组件配置等。

#### `src/router/` - 路由管理

管理应用的所有路由配置、权限控制、动态加载等，支持静态路由和动态路由。

#### `src/store/` - 状态管理

使用 Pinia 进行全局状态管理，支持数据持久化，包括用户、菜单、设置、表格、工作标签页等模块。

#### `src/utils/` - 工具函数

提供各种工具函数，包括 HTTP 请求、存储、路由、表单、表格、UI 等工具。

#### `src/views/` - 页面视图

应用的所有页面视图，对应路由配置，包括认证页面、仪表盘、系统管理、设备管理等。

## 🔑 关键文件说明

### 入口文件

- `src/main.ts` - 应用入口，初始化所有模块（Store、Router、国际化、指令等）
- `index.html` - HTML 入口文件
- `src/App.vue` - Vue 根组件，配置 Element Plus 国际化、主题初始化

### 配置文件

- `vite.config.ts` - Vite 构建配置（路径别名、构建选项、插件配置）
- `tsconfig.json` - TypeScript 编译配置（路径映射、编译选项）
- `src/config/index.ts` - 系统全局配置入口
- `src/config/setting.ts` - 系统设置默认配置

### 核心模块

- `src/router/index.ts` - 路由初始化
- `src/store/index.ts` - 状态管理初始化（Pinia 实例，持久化插件）
- `src/locales/index.ts` - 国际化初始化（vue-i18n）

## 📝 使用说明

### 开发环境

```bash
# 启动开发服务器（默认模式）
pnpm dev

# 开发环境模式启动
pnpm dev:dev

# 生产环境模式启动（用于测试生产环境配置）
pnpm dev:prod
```

### 构建生产

```bash
# 默认构建
pnpm build

# 开发环境模式构建
pnpm build:dev

# 生产环境模式构建
pnpm build:prod
```

### 代码检查

```bash
# 检查代码规范
pnpm lint

# 自动修复代码格式
pnpm fix

# Prettier 格式化
pnpm lint:prettier
```

### 提交代码

```bash
# 交互式提交（使用 cz-git）
pnpm commit
```

## 🔗 相关文档

- [Vite 官方文档](https://vitejs.dev/)
- [Vue 3 官方文档](https://cn.vuejs.org/)
- [Element Plus 文档](https://element-plus.org/)
- [Pinia 文档](https://pinia.vuejs.org/)
- [Vue Router 文档](https://router.vuejs.org/)
- [项目文档](https://www.artd.pro/docs/zh/guide/introduce.html)

---

**最后更新**：2025-12-03
